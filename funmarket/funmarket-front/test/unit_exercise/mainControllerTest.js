describe('mainController', function() {
    beforeEach(function(){
        //Module dependencies and injections
    });

    it('Should fetch marketads', function(){
        //Use $httpBackend to verify backend operations and mock the return data
    });

    afterEach(function() {
        //Something to do after each test?
    });
});