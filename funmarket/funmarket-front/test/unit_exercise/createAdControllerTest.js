describe('createAdController', function() {
    beforeEach(function(){
        //Module dependencies and injections
    });

    it('Will publish an ad', function(){
        //Use $httpBackend to verify backend operations
    });

    it('Will upload an image', function(){
        //Use $httpBackend to verify backend operations
    });

    afterEach(function() {
        //Something to do after each test?
    });

});